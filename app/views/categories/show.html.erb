<h1><%=@category.name.titleize%></h1>

<div class="category-creatures">
  <div class='row'>
    <% @category.creatures.active.each do |creature| %>
      <%= render partial: 'category_cards', locals: {creature: creature} %>
    <% end %>
  </div>
</div>
